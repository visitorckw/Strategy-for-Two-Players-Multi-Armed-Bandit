import game
import random

class agent():
    def play(self, agent, mahine_numbers, total_round, current_round, my_total_rewards, my_history_choice, opp_history_choice, my_history_reward):
        return int(random.random() * mahine_numbers)
